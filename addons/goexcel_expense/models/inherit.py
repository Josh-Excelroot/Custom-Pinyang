# from odoo import api, fields, models, _
#
# class Inherit(models.Model):
#     _inherit = "hr.expense"
#
#     def create(self, employee_id):
#         employee_id.update(employee_id=1)
#         return super(Inherit, self).create(employee_id)
